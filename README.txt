Requires psutil (can be installed through pip install psutil)
Requires playsound (can be installed through pip install playsound)
Requires pygobject (pip install pygobject)

THIS PROGRAM IS FOR WINDOWS ONLY(NO LINUX OR MACOS)